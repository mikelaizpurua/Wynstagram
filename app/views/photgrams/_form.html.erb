<%= form_for @photgram, html: { multipart: true } do |f| %>
  <% if @photgram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photgram.errors.count, "error") %> prohibited this photgram from being saved:</h2>
      <ul>
      <% @photgram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

  <div class="field">
    
    <%= f.file_field :image, class: 'el-upload',:autosave => true %>
  </div>
    <div class="field">
    <%= f.label :Comment %><br>
    <%= f.text_area :description, id: "textarea", :'maxlength' => "45" %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-success' %> 
  </div>
<% end %>
